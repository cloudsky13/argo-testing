apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-nr-marker-testing.fullname" . }}
  labels:
    {{- include "argo-nr-marker-testing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-nr-marker-testing.selectorLabels" . | nindent 4 }}
